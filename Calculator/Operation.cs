using System;

namespace WebCalculator
{
    public enum Operation
    {
        UnaryMinus = '_',
        Add = '+',
        Subtract = '-',
        Multiply = '*',
        Divide = '/',
        OpenBrace = '(',
        CloseBrace = ')'
    }
}
