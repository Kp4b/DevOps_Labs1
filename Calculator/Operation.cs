using System;

namespace Calculator
{
    public enum Operation
    {
        UnaryMinus = '_',
        Add = '+',
        Subtract = '-',
        Multiply = '*',
        Divide = '/',
        OpenBrace = '(',
        CloseBrace = ')'
    }
}
