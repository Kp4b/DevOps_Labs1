namespace WebCalculator.Core
{
    public enum Operation
    {
        UnaryMinus = '_',
        Add = '+',
        Subtract = '-',
        Multiply = '*',
        Divide = '/',
        OpenBrace = '(',
        CloseBrace = ')'
    }
}
